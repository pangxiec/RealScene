<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="60dp"
        android:orientation="vertical">
    <ImageView
            android:id="@+id/iv_location"
            android:src="@drawable/location_search"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginLeft="15dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

    <TextView
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/ll_location"
            android:id="@+id/sug_key"
            android:layout_marginBottom="2dp"
            app:layout_constraintVertical_chainStyle="packed"
            tools:text="hello"
            app:layout_constraintLeft_toRightOf="@+id/iv_location"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="15dp"
            android:textColor="#000000"
            android:textSize="14sp"/>

    <LinearLayout
            android:id="@+id/ll_location"
            app:layout_constraintLeft_toLeftOf="@+id/sug_key"
            app:layout_constraintTop_toBottomOf="@+id/sug_key"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

        <TextView
                android:id="@+id/sug_city"
                tools:text="heiha"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#696969"/>

        <TextView
                android:id="@+id/sug_dis"
                tools:text="lalal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#696969"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>